<UserControl x:Class="Regis.Controls.AchievementControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="64" d:DesignWidth="313" > <!--86-->
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="9*" />
            <RowDefinition Height="64*" />
            <RowDefinition Height="10*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*" />
            <ColumnDefinition Width="64*" />
            <ColumnDefinition Width="13*" />
            <ColumnDefinition Width="216*" />
            <ColumnDefinition Width="10*" />
        </Grid.ColumnDefinitions>
        <Image Source="{Binding AchvPicSrc}" Grid.Column="1" Grid.Row="1" ></Image>
        <TextBlock Text="{Binding AchvTxt}" Grid.Column="3" Grid.Row="1" ></TextBlock>  
        
    </Grid>
</UserControl>