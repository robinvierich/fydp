<UserControl x:Class="Regis.Views.FrequencyVsTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ctrls="clr-namespace:Regis.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <GroupBox Header="Hz / t">
            <ctrls:FrequencyVsTimeChart />
        </GroupBox>

        <GroupBox Header="Hz' / t" Grid.Column="1">
            <ctrls:FrequencyVsTimeChart />
        </GroupBox>

        <GroupBox Header="Hz'' / t" Grid.Column="2">
            <ctrls:FrequencyVsTimeChart />
        </GroupBox>
    </Grid>
</UserControl>
